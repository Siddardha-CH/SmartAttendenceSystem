# Copy this file to .env.local for local development
# For Render deployment, set VITE_API_URL in the frontend service env vars

# URL of your deployed Spring Boot backend (no trailing slash)
VITE_API_URL=https://your-backend.onrender.com/api
